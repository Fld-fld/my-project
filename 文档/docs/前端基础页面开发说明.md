# 前端基础页面开发说明

## 1. 前端项目目录结构

```
frontend/
├── src/
│   ├── main.js                        # 入口文件
│   ├── App.vue                        # 根组件
│   ├── api/                           # API接口
│   │   └── index.js                  # 所有接口定义
│   ├── router/                        # 路由配置
│   │   └── index.js                  # 路由表
│   ├── store/                         # 状态管理
│   │   ├── index.js                  # Pinia配置
│   │   └── user.js                   # 用户状态
│   ├── utils/                         # 工具函数
│   │   └── request.js                # Axios封装
│   └── views/                         # 页面组件
│       ├── Login.vue                  # ✅ 登录页
│       ├── Register.vue               # ✅ 注册页
│       ├── AdminLogin.vue             # ✅ 管理员登录页
│       ├── Layout.vue                 # ✅ 用户端布局
│       ├── Home.vue                   # ✅ 首页
│       ├── MyExpress.vue             # ✅ 我的快递
│       ├── Reservation.vue           # ✅ 取件预约
│       ├── Notifications.vue         # ✅ 消息通知
│       ├── Profile.vue               # ✅ 个人中心
│       ├── AdminLayout.vue           # ✅ 管理端布局
│       └── admin/                    # 管理员页面
│           ├── Home.vue              # ✅ 后台首页
│           ├── ExpressAdd.vue        # ✅ 录入快递
│           ├── ExpressManage.vue     # ✅ 快递管理
│           └── ReservationManage.vue # ✅ 预约管理
├── index.html                        # HTML模板
├── package.json                      # 依赖配置
└── vite.config.js                   # Vite配置
```

## 2. 已完成页面列表

| 页面 | 路径 | 状态 | 说明 |
| :--- | :--- | :--- | :--- |
| 登录 | /login | ✅ | 用户登录 |
| 注册 | /register | ✅ | 用户注册 |
| 管理员登录 | /admin/login | ✅ | 管理员登录 |
| 首页 | /home | ✅ | 快递查询、快捷入口 |
| 我的快递 | /my-express | ✅ | 待取/已取列表 |
| 取件预约 | /reservation | ✅ | 创建/取消预约 |
| 消息通知 | /notifications | ✅ | 通知列表、标记已读 |
| 个人中心 | /profile | ✅ | 用户信息、退出登录 |
| 后台首页 | /admin/home | ✅ | 统计数据面板 |
| 录入快递 | /admin/express/add | ✅ | 录入新快递 |
| 快递管理 | /admin/express/manage | ✅ | 查询、更新状态 |
| 预约管理 | /admin/reservation/manage | ✅ | 预约列表、确认取件 |

## 3. 前端路由配置表

| 路由 | 组件 | 权限 | 说明 |
| :--- | :--- | :--- | :--- |
| /login | Login.vue | 公开 | 用户登录 |
| /register | Register.vue | 公开 | 用户注册 |
| /admin/login | AdminLogin.vue | 公开 | 管理员登录 |
| / | Layout.vue | 需登录 | 用户端布局 |
| /home | Home.vue | 需登录 | 首页 |
| /my-express | MyExpress.vue | 需登录 | 我的快递 |
| /reservation | Reservation.vue | 需登录 | 取件预约 |
| /notifications | Notifications.vue | 需登录 | 消息通知 |
| /profile | Profile.vue | 需登录 | 个人中心 |
| /admin | AdminLayout.vue | 需管理员 | 管理端布局 |
| /admin/home | admin/Home.vue | 需管理员 | 后台首页 |
| /admin/express/add | admin/ExpressAdd.vue | 需管理员 | 录入快递 |
| /admin/express/manage | admin/ExpressManage.vue | 需管理员 | 快递管理 |
| /admin/reservation/manage | admin/ReservationManage.vue | 需管理员 | 预约管理 |

## 4. Axios请求封装说明

### 4.1 请求拦截器

```javascript
// 自动添加Token到请求头
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})
```

### 4.2 响应拦截器

```javascript
// 统一处理错误
request.interceptors.response.use(
  response => {
    if (res.code !== 200) {
      ElMessage.error(res.message)
      return Promise.reject(...)
    }
    return res
  },
  error => {
    if (error.response.status === 401) {
      // 登录过期，跳转登录页
    }
    return Promise.reject(error)
  }
)
```

### 4.3 API调用示例

```javascript
import { authAPI } from '../api'

// 登录
const res = await authAPI.login({ phone: '13800138001', password: '123456' })
const { token, user } = res.data
```

## 5. 跨域问题解决方案

### 5.1 后端配置 (CorsConfig.java)

```java
@Configuration
public class CorsConfig {
    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.addAllowedOriginPattern("*");  // 允许所有来源
        config.addAllowedHeader("*");
        config.addAllowedMethod("*");
        config.setAllowCredentials(true);
        return new CorsFilter(source);
    }
}
```

### 5.2 前端代理配置 (vite.config.js)

```javascript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

## 6. 路由守卫实现

```javascript
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  
  if (to.meta.requiresAuth && !token) {
    next('/login')
    return
  }
  
  if (to.meta.isAdmin) {
    const user = JSON.parse(localStorage.getItem('userInfo'))
    if (user.role !== 'ADMIN') {
      next('/home')
      return
    }
  }
  
  next()
})
```

## 7. Element UI组件使用

| 组件 | 用途 |
| :--- | :--- |
| el-button | 按钮 |
| el-input | 输入框 |
| el-select | 下拉选择 |
| el-tabs | 标签页 |
| el-dialog | 弹窗 |
| el-table | 表格 |
| el-form | 表单 |
| el-message | 消息提示 |
| el-loading | 加载动画 |

## 8. 启动说明

```bash
# 1. 安装依赖
cd frontend
npm install

# 2. 启动开发服务器
npm run dev

# 3. 访问地址
http://localhost:5173
```
