# 校园快递取件优化系统 - 系统详细设计文档

## 1. 总体架构设计

### 1.1 系统分层架构

```
─────────────────────────────────────────────────────────┐
│                    表现层 (Presentation Layer)           │
│                   Vue.js + Element UI                    │
│              (用户界面、交互逻辑、数据展示)                │
└─────────────────────────────────────────────────────────┘
                            ↓ HTTP/REST
┌─────────────────────────────────────────────────────────┐
│                    控制层 (Controller Layer)             │
│               Spring Boot @RestController                │
│           (请求接收、参数验证、响应返回、路由分发)          │
└─────────────────────────────────────────────────────────┘
                            ↓ 调用
┌─────────────────────────────────────────────────────────┐
│                    业务层 (Service Layer)                │
│              Spring Boot @Service + @Transactional       │
│         (业务逻辑、事务管理、数据校验、规则引擎)           │
└─────────────────────────────────────────────────────────┘
                            ↓ 调用
┌─────────────────────────────────────────────────────────┐
│                 数据访问层 (Repository Layer)            │
│              Spring Data JPA Repository                  │
│              (CRUD 操作、复杂查询、数据持久化)              │
└─────────────────────────────────────────────────────────┘
                            ↓ JDBC
┌─────────────────────────────────────────────────────────┐
│                    数据库层 (Database Layer)             │
│                      MySQL 8.0                           │
│                  (数据存储、索引、事务)                    │
└─────────────────────────────────────────────────────────┘
```

### 1.2 各层职责说明

| 层级 | 职责 | 技术实现 |
| :--- | :--- | :--- |
| **表现层** | 用户界面展示、用户交互、数据可视化 | Vue.js 组件、Element UI、Axios |
| **控制层** | 接收 HTTP 请求、参数验证、调用业务层、返回响应 | Spring MVC、@RestController、@Validated |
| **业务层** | 核心业务逻辑、事务管理、数据校验、业务规则 | @Service、@Transactional、自定义业务逻辑 |
| **数据访问层** | 数据库 CRUD 操作、复杂查询、数据映射 | Spring Data JPA、@Repository、JPQL |
| **数据库层** | 数据持久化存储、索引优化、事务支持 | MySQL InnoDB 引擎、索引、外键约束 |

### 1.3 技术架构图

```
                    ┌──────────────┐
                    │   用户浏览器  │
                    └──────┬───────┘
                           │ HTTP/HTTPS
                    ┌──────▼───────┐
                    │  Nginx 反向代理 │ (可选)
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼────────  ┌──────▼──────  ┌───────▼────────┐
│  前端静态资源   │  │  Spring Boot │  │    Redis 缓存   │
│  (Vue.js)      │  │  应用服务器   │  │   (可选)       │
└────────────────  └──────┬──────  └────────────────┘
                           │
                    ┌──────▼───────┐
                    │    MySQL     │
                    │   数据库      │
                    ──────────────┘
```

---

## 2. 核心功能模块详细设计

### 2.1 用户登录注册模块

#### 2.1.1 处理流程

```
用户注册流程：
开始 → 填写注册信息 → 前端验证 → 提交后端 → 验证学号/手机号唯一性 
→ 密码加密 (BCrypt) → 保存用户数据 → 返回成功 → 结束

用户登录流程：
开始 → 输入手机号/密码 → 前端验证 → 提交后端 → 验证用户存在 
→ 密码匹配验证 → 生成 JWT Token → 返回用户信息+Token → 结束
```

#### 2.1.2 类设计

```java
// 用户实体类
@Entity
@Table(name = "user")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "student_id", unique = true, nullable = false)
    private String studentId;
    
    @Column(name = "name", nullable = false)
    private String name;
    
    @Column(name = "phone", unique = true, nullable = false)
    private String phone;
    
    @Column(name = "password", nullable = false)
    private String password; // BCrypt 加密存储
    
    @Column(name = "role")
    private String role = "STUDENT"; // STUDENT/ADMIN
    
    @Column(name = "created_at")
    private LocalDateTime createdAt;
    
    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
}

// 用户 DTO
public class UserDTO {
    private Long id;
    private String studentId;
    private String name;
    private String phone;
    private String role;
}

// 登录请求 DTO
public class LoginRequest {
    @NotBlank(message = "手机号不能为空")
    @Pattern(regexp = "^1[3-9]\\d{9}$", message = "手机号格式不正确")
    private String phone;
    
    @NotBlank(message = "密码不能为空")
    private String password;
}

// 登录响应 DTO
public class LoginResponse {
    private String token;
    private UserDTO user;
}

// 注册请求 DTO
public class RegisterRequest {
    @NotBlank
    private String studentId;
    
    @NotBlank
    private String name;
    
    @NotBlank
    @Pattern(regexp = "^1[3-9]\\d{9}$")
    private String phone;
    
    @NotBlank
    @Size(min = 6, max = 16)
    private String password;
}
```

#### 2.1.3 关键算法

**密码加密算法（BCrypt）**：
```java
@Service
public class PasswordEncoder {
    private final BCryptPasswordEncoder encoder = new BCryptPasswordEncoder(10);
    
    public String encode(String rawPassword) {
        return encoder.encode(rawPassword);
    }
    
    public boolean matches(String rawPassword, String encodedPassword) {
        return encoder.matches(rawPassword, encodedPassword);
    }
}
```

**JWT Token 生成算法**：
```java
@Component
public class JwtTokenProvider {
    
    @Value("${jwt.secret}")
    private String jwtSecret;
    
    @Value("${jwt.expiration}")
    private long jwtExpiration;
    
    public String generateToken(User user) {
        Claims claims = Jwts.claims();
        claims.put("userId", user.getId());
        claims.put("role", user.getRole());
        
        return Jwts.builder()
                .setClaims(claims)
                .setSubject(user.getPhone())
                .setIssuedAt(new Date())
                .setExpiration(new Date(System.currentTimeMillis() + jwtExpiration))
                .signWith(SignatureAlgorithm.HS256, jwtSecret)
                .compact();
    }
}
```

---

### 2.2 快递信息管理模块

#### 2.2.1 录入流程

```
管理员录入快递：
开始 → 登录后台 → 填写快递信息 → 验证数据完整性 → 生成唯一取件码 
→ 保存快递记录 → 发送通知给用户 → 结束

批量导入：
开始 → 上传 Excel 文件 → 解析文件内容 → 逐条验证数据 
→ 批量保存 → 返回导入结果 → 结束
```

#### 2.2.2 查询流程

```
用户查询快递：
开始 → 用户登录 → 获取用户手机号 → 查询关联快递列表 
→ 按状态/时间排序 → 返回快递列表 → 结束

管理员查询快递：
开始 → 输入查询条件（单号/姓名/状态）→ 执行查询 → 返回结果列表 → 结束
```

#### 2.2.3 类设计

```java
// 快递实体类
@Entity
@Table(name = "express")
public class Express {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "tracking_number", unique = true, nullable = false)
    private String trackingNumber; // 快递单号
    
    @Column(name = "company", nullable = false)
    private String company; // 快递公司
    
    @Column(name = "recipient_name", nullable = false)
    private String recipientName; // 收件人姓名
    
    @Column(name = "recipient_phone", nullable = false)
    private String recipientPhone; // 收件人手机号
    
    @Column(name = "location", nullable = false)
    private String location; // 存放位置
    
    @Column(name = "pickup_code", unique = true, nullable = false)
    private String pickupCode; // 取件码
    
    @Column(name = "status", nullable = false)
    private String status = "PENDING"; // PENDING/RESERVED/PICKED_UP/EXCEPTION
    
    @Column(name = "admin_id")
    private Long adminId; // 录入管理员 ID
    
    @Column(name = "created_at")
    private LocalDateTime createdAt;
    
    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
}

// 快递状态枚举
public enum ExpressStatus {
    PENDING("待取件"),
    RESERVED("已预约"),
    PICKED_UP("已取件"),
    EXCEPTION("异常");
    
    private final String description;
}
```

#### 2.2.4 取件码生成算法

```java
@Service
public class PickupCodeGenerator {
    
    /**
     * 生成 6 位数字取件码
     * 规则：时间戳后 6 位 + 随机数
     */
    public String generatePickupCode() {
        String timestamp = String.valueOf(System.currentTimeMillis());
        String timePart = timestamp.substring(timestamp.length() - 4);
        String randomPart = String.format("%02d", new Random().nextInt(100));
        return timePart + randomPart;
    }
    
    /**
     * 验证取件码唯一性
     */
    @Autowired
    private ExpressRepository expressRepository;
    
    public boolean isPickupCodeUnique(String pickupCode) {
        return !expressRepository.existsByPickupCode(pickupCode);
    }
}
```

---

### 2.3 取件预约模块

#### 2.3.1 预约创建流程

```
用户创建预约：
开始 → 选择快递 → 选择预约日期 → 选择时间段 → 验证时段可用性 
→ 检查预约限制 → 保存预约记录 → 更新快递状态 → 发送确认通知 → 结束
```

#### 2.3.2 预约取消流程

```
用户取消预约：
开始 → 查看预约列表 → 选择要取消的预约 → 验证取消时间限制 
→ 更新预约状态 → 更新快递状态 → 发送取消通知 → 结束
```

#### 2.3.3 类设计

```java
// 预约实体类
@Entity
@Table(name = "reservation")
public class Reservation {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "user_id", nullable = false)
    private Long userId;
    
    @Column(name = "express_id", nullable = false)
    private Long expressId;
    
    @Column(name = "reservation_date", nullable = false)
    private LocalDate reservationDate;
    
    @Column(name = "start_time", nullable = false)
    private LocalTime startTime;
    
    @Column(name = "end_time", nullable = false)
    private LocalTime endTime;
    
    @Column(name = "status", nullable = false)
    private String status = "PENDING"; // PENDING/CONFIRMED/CANCELLED/COMPLETED
    
    @Column(name = "created_at")
    private LocalDateTime createdAt;
    
    @Column(name = "updated_at")
    private LocalDateTime updatedAt;
    
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", insertable = false, updatable = false)
    private User user;
    
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "express_id", insertable = false, updatable = false)
    private Express express;
}

// 预约请求 DTO
public class ReservationRequest {
    @NotNull
    private Long expressId;
    
    @NotNull
    @FutureOrPresent(message = "预约日期必须是将来的日期")
    private LocalDate reservationDate;
    
    @NotNull
    private LocalTime startTime;
    
    @NotNull
    private LocalTime endTime;
}

// 时段容量配置
@Configuration
@ConfigurationProperties(prefix = "reservation")
public class ReservationConfig {
    private int maxCapacityPerSlot = 20; // 每个时段最大预约人数
    private int cancelHoursLimit = 1; // 取消预约提前小时数
    private int modifyHoursLimit = 2; // 修改预约提前小时数
}
```

#### 2.3.4 时段可用性验证

```java
@Service
public class ReservationService {
    
    @Autowired
    private ReservationRepository reservationRepository;
    
    @Value("${reservation.max-capacity-per-slot}")
    private int maxCapacity;
    
    /**
     * 检查时段是否可预约
     */
    public boolean isSlotAvailable(LocalDate date, LocalTime startTime, LocalTime endTime) {
        long count = reservationRepository.countByDateAndTimeRange(date, startTime, endTime);
        return count < maxCapacity;
    }
    
    /**
     * 验证预约时间限制
     */
    public boolean canReserve(LocalDateTime reservationTime) {
        LocalDateTime now = LocalDateTime.now();
        return reservationTime.isAfter(now);
    }
    
    /**
     * 验证取消时间限制
     */
    public boolean canCancel(LocalDateTime reservationTime) {
        LocalDateTime now = LocalDateTime.now();
        long hoursUntilReservation = ChronoUnit.HOURS.between(now, reservationTime);
        return hoursUntilReservation >= 1; // 提前 1 小时可取消
    }
}
```

---

### 2.4 消息通知模块

#### 2.4.1 通知类型

| 通知类型 | 触发时机 | 通知内容 | 通知方式 |
| :--- | :--- | :--- | :--- |
| **快递到达通知** | 管理员录入快递后 | 快递单号、取件码、存放位置 | 站内信 + 短信 |
| **预约确认通知** | 用户成功预约后 | 预约时间、快递信息 | 站内信 |
| **取件提醒通知** | 预约时间前 1 小时 | 预约时间、取件码、位置 | 站内信 |
| **预约取消通知** | 用户/管理员取消预约 | 取消原因、快递信息 | 站内信 |
| **系统通知** | 系统维护、升级等 | 通知标题、内容 | 站内信 |

#### 2.4.2 通知触发机制

```java
// 通知实体类
@Entity
@Table(name = "notification")
public class Notification {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "user_id", nullable = false)
    private Long userId;
    
    @Column(name = "type", nullable = false)
    private String type; // ARRIVAL/RESERVATION_CONFIRM/REMINDER/CANCELLED/SYSTEM
    
    @Column(name = "title", nullable = false)
    private String title;
    
    @Column(name = "content", nullable = false)
    private String content;
    
    @Column(name = "is_read", nullable = false)
    private Boolean isRead = false;
    
    @Column(name = "created_at")
    private LocalDateTime createdAt;
}

// 通知服务
@Service
public class NotificationService {
    
    @Autowired
    private NotificationRepository notificationRepository;
    
    /**
     * 发送快递到达通知
     */
    @Transactional
    public void sendArrivalNotification(User user, Express express) {
        Notification notification = new Notification();
        notification.setUserId(user.getId());
        notification.setType("ARRIVAL");
        notification.setTitle("您的快递已到达");
        notification.setContent(String.format(
            "快递单号：%s\n快递公司：%s\n取件码：%s\n存放位置：%s",
            express.getTrackingNumber(),
            express.getCompany(),
            express.getPickupCode(),
            express.getLocation()
        ));
        notification.setCreatedAt(LocalDateTime.now());
        
        notificationRepository.save(notification);
        
        // 可选：发送短信通知
        // smsService.send(user.getPhone(), notification.getContent());
    }
    
    /**
     * 发送预约确认通知
     */
    @Transactional
    public void sendReservationConfirmation(User user, Reservation reservation) {
        Notification notification = new Notification();
        notification.setUserId(user.getId());
        notification.setType("RESERVATION_CONFIRM");
        notification.setTitle("预约成功");
        notification.setContent(String.format(
            "您已成功预约取件\n时间：%s %s-%s\n请尽快取件",
            reservation.getReservationDate(),
            reservation.getStartTime(),
            reservation.getEndTime()
        ));
        notification.setCreatedAt(LocalDateTime.now());
        
        notificationRepository.save(notification);
    }
}
```

---

### 2.5 管理员后台模块

#### 2.5.1 数据统计

```java
// 统计数据 DTO
public class StatisticsDTO {
    private Long totalExpress; // 总快递数
    private Long pendingExpress; // 待取件数
    private Long pickedUpExpress; // 已取件数
    private Double pickupRate; // 取件率
    private Long todayArrivals; // 今日到件数
    private Long todayReservations; // 今日预约数
    private Map<String, Long> hourlyDistribution; // 时段分布
}

// 统计服务
@Service
public class StatisticsService {
    
    @Autowired
    private ExpressRepository expressRepository;
    
    @Autowired
    private ReservationRepository reservationRepository;
    
    /**
     * 获取今日统计数据
     */
    public StatisticsDTO getTodayStatistics() {
        LocalDate today = LocalDate.now();
        LocalDateTime startOfDay = today.atStartOfDay();
        LocalDateTime endOfDay = today.atTime(23, 59, 59);
        
        StatisticsDTO stats = new StatisticsDTO();
        stats.setTodayArrivals(expressRepository.countByCreatedAtBetween(startOfDay, endOfDay));
        stats.setTodayReservations(reservationRepository.countByReservationDate(today));
        
        // 获取总体统计
        stats.setTotalExpress(expressRepository.count());
        stats.setPendingExpress(expressRepository.countByStatus("PENDING"));
        stats.setPickedUpExpress(expressRepository.countByStatus("PICKED_UP"));
        
        Double pickupRate = stats.getTotalExpress() > 0 
            ? (stats.getPickedUpExpress() * 100.0 / stats.getTotalExpress())
            : 0.0;
        stats.setPickupRate(pickupRate);
        
        return stats;
    }
}
```

#### 2.5.2 权限管理

```java
// 权限注解
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RequireRole {
    String[] value(); // 允许访问的角色
}

// 权限切面
@Aspect
@Component
public class PermissionAspect {
    
    @Around("@annotation(requireRole)")
    public Object checkPermission(ProceedingJoinPoint pjp, RequireRole requireRole) throws Throwable {
        // 获取当前用户角色
        String currentUserRole = SecurityContextHolder.getCurrentUserRole();
        
        // 验证权限
        if (!Arrays.asList(requireRole.value()).contains(currentUserRole)) {
            throw new AccessDeniedException("权限不足");
        }
        
        return pjp.proceed();
    }
}

// 使用示例
@RestController
@RequestMapping("/api/admin")
public class AdminController {
    
    @PostMapping("/express")
    @RequireRole({"ADMIN", "SUPER_ADMIN"})
    public ResponseEntity<?> addExpress(@RequestBody ExpressRequest request) {
        // 录入快递逻辑
    }
}
```

---

## 3. 接口设计

### 3.1 RESTful API 规范

#### 3.1.1 接口命名规范

- 使用名词复数形式：`/api/users`, `/api/expresses`
- 资源嵌套使用子路径：`/api/users/{userId}/reservations`
- 使用小写字母，单词间用连字符：`/api/pickup-codes`

#### 3.1.2 HTTP 方法规范

| 方法 | 用途 | 幂等性 |
| :--- | :--- | :--- |
| GET | 查询资源 | 是 |
| POST | 创建资源 | 否 |
| PUT | 更新资源（全量） | 是 |
| PATCH | 更新资源（部分） | 是 |
| DELETE | 删除资源 | 是 |

#### 3.1.3 统一响应格式

```java
// 统一响应体
@Data
public class ApiResponse<T> {
    private Integer code;
    private String message;
    private T data;
    private Long timestamp;
    
    public static <T> ApiResponse<T> success(T data) {
        ApiResponse<T> response = new ApiResponse<>();
        response.setCode(200);
        response.setMessage("success");
        response.setData(data);
        response.setTimestamp(System.currentTimeMillis());
        return response;
    }
    
    public static <T> ApiResponse<T> error(Integer code, String message) {
        ApiResponse<T> response = new ApiResponse<>();
        response.setCode(code);
        response.setMessage(message);
        response.setTimestamp(System.currentTimeMillis());
        return response;
    }
}
```

### 3.2 核心接口列表

#### 3.2.1 用户认证接口

| 接口地址 | 方法 | 描述 | 请求参数 | 响应数据 |
| :--- | :--- | :--- | :--- | :--- |
| `/api/auth/register` | POST | 用户注册 | studentId, name, phone, password | userId |
| `/api/auth/login` | POST | 用户登录 | phone, password | token, user |
| `/api/auth/logout` | POST | 用户退出 | - | - |
| `/api/auth/refresh` | POST | 刷新 Token | refreshToken | token |

**接口示例：用户登录**
```http
POST /api/auth/login
Content-Type: application/json

{
  "phone": "13800138000",
  "password": "password123"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "studentId": "2021001234",
      "name": "张三",
      "phone": "13800138000",
      "role": "STUDENT"
    }
  },
  "timestamp": 1717776000000
}
```

#### 3.2.2 快递管理接口

| 接口地址 | 方法 | 描述 | 请求参数 | 响应数据 |
| :--- | :--- | :--- | :--- | :--- |
| `/api/express` | GET | 获取快递列表（用户） | page, size, status | 快递列表 |
| `/api/express/{id}` | GET | 获取快递详情 | - | 快递详情 |
| `/api/express` | POST | 录入快递（管理员） | trackingNumber, company, recipientName, recipientPhone, location | expressId |
| `/api/express/{id}` | PUT | 更新快递信息 | trackingNumber, company, location, status | - |
| `/api/express/{id}` | DELETE | 删除快递 | - | - |
| `/api/express/batch-import` | POST | 批量导入快递 | file (Excel) | 导入结果 |

#### 3.2.3 预约管理接口

| 接口地址 | 方法 | 描述 | 请求参数 | 响应数据 |
| :--- | :--- | :--- | :--- | :--- |
| `/api/reservations` | GET | 获取预约列表 | page, size, status | 预约列表 |
| `/api/reservations` | POST | 创建预约 | expressId, reservationDate, startTime, endTime | reservationId |
| `/api/reservations/{id}` | PUT | 更新预约 | reservationDate, startTime, endTime | - |
| `/api/reservations/{id}` | DELETE | 取消预约 | - | - |
| `/api/reservations/slots` | GET | 获取可预约时段 | date | 时段列表 |

#### 3.2.4 通知管理接口

| 接口地址 | 方法 | 描述 | 请求参数 | 响应数据 |
| :--- | :--- | :--- | :--- | :--- |
| `/api/notifications` | GET | 获取通知列表 | page, size, isRead | 通知列表 |
| `/api/notifications/{id}/read` | PUT | 标记已读 | - | - |
| `/api/notifications/unread-count` | GET | 获取未读数量 | - | count |

#### 3.2.5 管理员接口

| 接口地址 | 方法 | 描述 | 请求参数 | 响应数据 |
| :--- | :--- | :--- | :--- | :--- |
| `/api/admin/statistics` | GET | 获取统计数据 | date | 统计数据 |
| `/api/admin/users` | GET | 获取用户列表 | page, size | 用户列表 |
| `/api/admin/users/{id}/disable` | POST | 禁用用户 | - | - |
| `/api/admin/reservations` | GET | 获取所有预约 | page, size, date | 预约列表 |

### 3.3 错误码说明

| 错误码 | 含义 | 处理建议 |
| :--- | :--- | :--- |
| 200 | 成功 | - |
| 400 | 请求参数错误 | 检查请求参数格式 |
| 401 | 未授权 | 重新登录 |
| 403 | 权限不足 | 联系管理员 |
| 404 | 资源不存在 | 检查资源 ID |
| 409 | 资源冲突（如重复） | 修改请求数据 |
| 500 | 服务器内部错误 | 联系技术支持 |

---

## 4. 安全设计

### 4.1 用户认证（JWT）

#### 4.1.1 JWT 结构

```
JWT Header:
{
  "alg": "HS256",
  "typ": "JWT"
}

JWT Payload:
{
  "userId": 1,
  "role": "STUDENT",
  "sub": "13800138000",
  "iat": 1717776000,
  "exp": 1717862400
}

JWT Signature:
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  secret
)
```

#### 4.1.2 Token 配置

```yaml
jwt:
  secret: "your-secret-key-at-least-32-characters-long"
  expiration: 86400000 # 24 小时
  refresh-expiration: 604800000 # 7 天
```

#### 4.1.3 认证过滤器

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    @Autowired
    private JwtTokenProvider tokenProvider;
    
    @Autowired
    private UserDetailsService userDetailsService;
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                    HttpServletResponse response, 
                                    FilterChain filterChain) {
        String token = resolveToken(request);
        
        if (token != null && tokenProvider.validateToken(token)) {
            Authentication auth = tokenProvider.getAuthentication(token);
            SecurityContextHolder.getContext().setAuthentication(auth);
        }
        
        filterChain.doFilter(request, response);
    }
    
    private String resolveToken(HttpServletRequest request) {
        String bearerToken = request.getHeader("Authorization");
        if (bearerToken != null && bearerToken.startsWith("Bearer ")) {
            return bearerToken.substring(7);
        }
        return null;
    }
}
```

### 4.2 权限控制

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf().disable()
            .authorizeHttpRequests()
                .antMatchers("/api/auth/**").permitAll()
                .antMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            .and()
            .sessionManagement()
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS);
        
        return http.build();
    }
}
```

### 4.3 数据加密

#### 4.3.1 密码加密

- 使用 BCrypt 强哈希算法
- Salt 长度：10
- 加密强度：中等

#### 4.3.2 数据传输加密

- 生产环境强制使用 HTTPS
- 敏感数据（密码、手机号）前端加密传输
- 使用 TLS 1.2+ 协议

---

## 5. 异常处理设计

### 5.1 全局异常处理器

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    /**
     * 业务异常
     */
    @ExceptionHandler(BusinessException.class)
    public ApiResponse<?> handleBusinessException(BusinessException e) {
        return ApiResponse.error(e.getCode(), e.getMessage());
    }
    
    /**
     * 参数验证异常
     */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ApiResponse<?> handleValidationException(MethodArgumentNotValidException e) {
        BindingResult bindingResult = e.getBindingResult();
        String message = bindingResult.getFieldError().getDefaultMessage();
        return ApiResponse.error(400, message);
    }
    
    /**
     * 权限不足异常
     */
    @ExceptionHandler(AccessDeniedException.class)
    public ApiResponse<?> handleAccessDeniedException(AccessDeniedException e) {
        return ApiResponse.error(403, "权限不足");
    }
    
    /**
     * 未授权异常
     */
    @ExceptionHandler(AuthenticationException.class)
    public ApiResponse<?> handleAuthenticationException(AuthenticationException e) {
        return ApiResponse.error(401, "未授权");
    }
    
    /**
     * 系统异常
     */
    @ExceptionHandler(Exception.class)
    public ApiResponse<?> handleException(Exception e) {
        e.printStackTrace();
        return ApiResponse.error(500, "服务器内部错误");
    }
}
```

### 5.2 自定义业务异常

```java
public class BusinessException extends RuntimeException {
    
    private Integer code;
    
    public BusinessException(String message) {
        super(message);
        this.code = 400;
    }
    
    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
    }
    
    public static BusinessException badRequest(String message) {
        return new BusinessException(400, message);
    }
    
    public static BusinessException notFound(String message) {
        return new BusinessException(404, message);
    }
    
    public static BusinessException conflict(String message) {
        return new BusinessException(409, message);
    }
}
```

### 5.3 错误信息统一返回格式

```json
{
  "code": 400,
  "message": "手机号格式不正确",
  "data": null,
  "timestamp": 1717776000000
}
```

---

**文档版本**：V1.0  
**创建日期**：2026 年 6 月 7 日  
**团队**：极速取件小组